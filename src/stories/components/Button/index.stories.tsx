import React from 'react';
import { Button } from '.';

export const Primary = () => {
  return <Button.Primary>Primary</Button.Primary>;
};

export const Secondary = () => {
  return <Button.Secondary>Secondary</Button.Secondary>;
};
